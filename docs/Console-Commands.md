# Console Commands

The application interfaces allow for certain operations to be performed from the console in order to allow easy integration into shell scripts and administration.

The commands are either frontend `frontend/yii [commands]` or backend `backend/yii [commands]`.
