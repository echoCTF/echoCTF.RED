Introduce a JS override that will be loaded on all pages of your platform.

Enter a list of relative paths or full URLs of JS files to be loaded (one per-line).
Alternatively you can include the actual content of Javascript by putting the
following **`/* ... */`** as a first line of the content.
