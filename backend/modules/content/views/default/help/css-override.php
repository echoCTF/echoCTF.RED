Introduce a CSS override that will be loaded on all pages of your platform.

Enter a list of relative paths or full URLs of CSS files to be loaded (one per-line).
Alternatively you can include the actual content of the CSS by putting the
following **`/* ... */`** as a first line of the content.
