Create new email template that can be used by the platform or your custom modules and scripts.

The form fields include:
* `Name`: The internal name that the platform uses to find this mail template
* `Title`: The email subject link
* `Html`: The HTML body of the email
* `Txt`: The plain text body of the email

The HTML and TXT version have the current player object assigned as the variable `$user`.